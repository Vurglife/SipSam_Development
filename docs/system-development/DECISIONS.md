# Decisions

| Date | Decision | Reason | Alternatives Rejected | Revisit Trigger |
| --- | --- | --- | --- | --- |
| 2026-05-26 | Roulette inside bets use a mobile-safe precision rail for Split, Street, Corner, and Line while the table layout remains one-tap for straight and outside bets. The server validates the true American table geometry for every inside bet. | Phone players need reliable compound bet placement without tiny edge/intersection hit targets, and payout integrity cannot trust client-selected target arrays. | Expose only straight/outside bets; use invisible table-edge hitboxes as the first mobile interaction; accept split/street/corner/line arrays without server geometry checks. | If desktop-first casino-table hitboxes are added later, or user testing shows the precision rail slows down common betting patterns. |
| 2026-05-26 | Roulette's first playable release is American-only. European roulette remains deferred, with helper logic retained but product entry, client UI, matchmake, room creation, quick-join, and room state all coercing to American. | The product direction is to finish American Roulette first using the attached double-zero wheel/rules, then build European separately later. Hiding European now avoids exposing the wrong rule set or house-edge copy before it is intentionally designed and tested. | Let players choose American vs European in the first release; rely only on the client default while the server still accepts European rooms. | When Mitstar is ready to build the European Roulette table and its UI/rule copy intentionally. |
| 2026-05-26 | Roulette wallet movement is platform/server-authoritative: dashboard entry must stop if `/api/game/roulette/enter` fails, replenish must run through the Roulette server to `/api/game/replenish`, and exits must settle by explicit exit, lobby back, unload beacon, or server disconnect. Locked spin stakes are deducted from exit settlement before resolution. | Roulette moves durable bank balances, and the old prototype could continue after a failed bank draw, mint local wallet chips on replenish, and lose or over-return money on close. Server-side settlement and beacon fallback match the safer Rhum32/Blackjack pattern. | Client-only replenish; continuing to Roulette when platform entry fails; relying only on explicit Exit; refunding locked spin stakes when a player closes mid-spin. | If wallet sessions move out of in-memory platform state, Roulette adds reconnect/resume, or table economics are centralized in a shared config module. |
| 2026-05-25 | Dashboard chip sending is bank-funded and lives primarily under Bank, with Friends Network shortcuts into the same flow. Recipients receive a friend inbox message and platform notification for every successful transfer. | Chip transfers move durable bank balances, so Bank should be the source of truth. Friends Network remains the natural recipient-picker entry point, but both paths must use one server-authoritative transfer endpoint. | Add a separate main menu item; send from active game wallets; implement a client-only transfer; allow sending to non-friends. | If wallet types are separated further, transfer limits/fees are added, or a full transaction history/inbox ledger is introduced. |
| 2026-05-22 | Rhum32 visual changes must preserve mobile phone and tablet usability: tap targets, betting controls, lobby cards, table metadata, chat/menu controls, and safe-area spacing are part of the styling acceptance criteria. | The game will be played on mobile devices, so a desktop-only casino treatment can make core betting and lobby flows harder to use. | Treat mobile as a later polish pass; rely only on existing responsive rules while changing colors and surfaces. | If phone-width browser review shows cramped controls, clipped text, or overlapping table/betting elements. |
| 2026-05-22 | Rhum32's visual system uses a high-contrast casino palette: near-black/velvet surfaces, ruby felt, champagne-gold value emphasis, ivory body text, and emerald only for active/success states. | The previous burgundy/gold/green mix made text, controls, table graphics, and status cues too similar in the mode, lobby, and in-game screens. Separating neutral text, money emphasis, table felt, and state cues improves readability while keeping a premium casino identity. | Continue using muted green as a broad secondary color; make the entire game a single burgundy/gold theme; redesign the DOM/layout before fixing contrast. | If logged-in browser review shows any table/lobby text overlap, insufficient contrast, or a different casino art direction is requested. |
| 2026-05-22 | Rhum32 Table 4 payments are table-tier aware and server-authoritative: Normal covers `$100/$500/$1,000`, VIP starts at `$10,000`, Elite at `$100,000`, and Celestial at `$250,000`; mid-round exits remain in the room until the hand settles, then the game server returns wallet to bank. | The supplied payout chart changes both bonus amounts and face-special multipliers by table tier, and wallet/bank correctness cannot depend on a client closing at the right time. Trusted server settlement can also correct client beacons after final payouts. | Keep the old static Rhum32 bonuses; disqualify players who cannot cover back bet from wallet; rely only on client `/rhum32/exit` during live rounds. | If Rhum32 table economics change again or wallet sessions move out of in-memory platform state. |
| 2026-05-21 | Rhum32 uses the six-tier Table 1 ladder: `$100`, `$500`, `$1,000`, `VIP $10,000`, `Elite $100,000`, and `Celestial $250,000`; front/tie increments must be synchronized through platform wallet config, dashboard tables, Rhum32 room state, client controls, and invite payloads. | A partial config update left the dashboard and client bet steps on the old five-table ladder. Keeping all mirrors synchronized prevents wrong eligibility, wrong wallet draw sizes, and odd client-forged bet amounts. | Keep the old `$5,000` table and "No Limit" tie bet display; rely only on the client to step bets correctly. | If the May 2026 Rhum32 Table 1 economics change or table config is centralized in a shared module. |
| 2026-05-21 | Blackjack's duplicated playable hand is a bottom-centered play tray; the table keeps only compact seat-context cards and a VurgLife center watermark. | Mobile Blackjack needs readable player cards and thumb-reachable actions without covering the dealer/table context. Split hands can sit side-by-side in the bottom tray while the active split is emphasized. | Keep the duplicate hand in the table center; make the player's table-seat cards the primary large cards. | If 375px live testing shows the bottom tray or split-hand scaling still overlaps actions, freeze, or table content. |
| 2026-05-21 | VurgLife friend messages use a dedicated `friend_messages` inbox table plus `/api/friends/messages` routes, not the legacy room `messages` table. | Friend-to-friend messages need inbox/sent/delete/unread behavior without mixing with game room chat records. | Reuse the existing `messages(room_id, user_id, username, content)` table. | If a unified platform messaging service replaces both room chat and friend DMs. |
| 2026-05-20 | Blackjack invite receivers join with an explicit guest role and are never host candidates; only non-guest seats can start a waiting multiplayer table. | Accepting an invite before the sender is recognized in the room must not make the invitee host. Guests should see only Cancel in the pre-game lobby and can still occupy an open seat if the table is already active. | Continue inferring host from first occupied seat only; rely on client-side hiding of the Start button. | If Blackjack adds persistent room ownership records or a full server-side lobby service. |
| 2026-05-20 | Blackjack includes plain fixed-bet `$500` and `$1,000` tables alongside `$100`, one `$50,000` VIP, `$100,000` Elite, and `$500,000` Celestial. | Product owner wants the mid-low Blackjack tables back, but not branded as VIP or called out as Standard in the dashboard. | Keep only `$100` before VIP; or reintroduce lower VIP tables. | If table economics are centralized in a shared Blackjack config module. |
| 2026-05-20 | Blackjack has exactly one VIP table at `$50,000`; lower `$1,000`, `$5,000`, `$10,000`, and `$20,000` VIP tables are removed. | The product direction is fewer, clearer premium Blackjack rooms: Standard, VIP, Elite, and Celestial. | Keep a VIP ladder for every intermediate Blackjack tier. | If the product owner asks for intermediate non-VIP Blackjack tables later. |
| 2026-05-20 | Blackjack premium dashboard tables mirror SipSam's VIP/Elite/Celestial visual language, and premium table keys are synchronized across dashboard, platform wallet API, and Blackjack WS server. | Enhanced table cards must not advertise rooms that the wallet route or WS server cannot open. Elite uses `$100,000`; Celestial uses `$500,000`, matching SipSam's high-tier anchors. | Apply CSS-only labels to existing Blackjack tables. | If Blackjack tier economics change or a shared cross-game table-config module is introduced. |
| 2026-05-20 | Blackjack client source of truth stays in external `blackjack-client/game.js`, `style.css`, `vl-card.css`, and markup in `index.html`; recovered inline style/script tails are removed. | The recovered inline tail was malformed and made menu/chat/layout fixes hard to reason about. External files support cache busting and focused validation. | Keep appending recovered inline patches to `index.html`. | If a build step or bundler replaces the current static-client model. |
| 2026-05-20 | Blackjack card backs use `blackjack-client/backVurgLife.png`, copied from the SipSam card back asset. | The product owner requested the VurgLife back graphic and Blackjack should visually match established VurgLife games. | Reference the poker-client asset across folders or keep the generic blue back. | If a shared assets folder is introduced for all games. |
| 2026-05-20 | Blackjack uses a duplicated center active-hand focus while still rendering cards at the player's seat. | Mobile play needs readable card values and action controls close to the active hand, while table context still requires cards at each seat. | Only enlarge seat cards, or move cards away from the seat entirely. | If 375px live testing shows overlap or center focus blocks needed table context. |
| 2026-05-15 | Apply the `system-developer-analyst` project template to PokerProject. | PokerProject is a multi-app, multi-session platform where durable memory, validation, and lessons reduce cost and repeated discovery. | Continue relying on chat history and scattered handoff documents only. | If the template becomes too heavy or is not being used during real work. |
| 2026-05-15 | Place the project memory in `docs/system-development`. | The repository root already contains many handoff and recovery files. A dedicated docs folder is cleaner and easier to find. | Add `BRIEF.md`, `DESIGN.md`, and related files directly to the root. | If the team standardizes on root-level planning files later. |
| 2026-05-15 | Do not change application code during template application. | The current task is to apply the project operating system, not modify game behavior. | Combine project setup with feature changes. | When a specific PokerProject development task is requested. |
| 2026-05-15 | Add a project-local Claude Code setup instead of starting with a reusable plugin. | Project-local `CLAUDE.md`, skills, agents, and settings are easier to validate in PokerProject before packaging reusable workflows. | Build a global Claude plugin first. | When the PokerProject setup proves stable and should be reused across multiple projects. |
| 2026-05-15 | Let Claude Code specialist agents make scoped edits when requested. | Mitstar wants Claude Code to handle implementation directly to reduce human error. Safety should come from project rules, validation, and permission settings, not read-only agents. | Keep all specialist agents read-only. | If agents make broad or unsafe edits, tighten individual agent permissions. |
| 2026-05-15 | Allow Claude Code to stage and commit current-task changes after validation. | Prior uncommitted work was lost; committing meaningful changes is part of the recovery-safe workflow. | Force all commits to be manual. | If branch strategy changes or commits need review gates. |
| 2026-05-17 | Use a project-scoped read-only SQLite MCP server for VurgLife analysis. | It unlocks schema and reporting workflows while keeping `vurglife.db` immutable from Claude Code. | Use direct DB edits or a write-capable community SQLite MCP server. | If analytics move to a separate reporting database or read replica. |
| 2026-05-17 | Package the proven PokerProject Claude setup as `vurglife-dev-os`. | Agents, skills, hooks, and MCP tooling should be portable without relying on old chats. | Keep all workflow assets project-local only. | If plugin validation or install behavior changes in Claude Code. |
| 2026-05-18 | Start tracking `rhum32-client/` in git. | Rhum32 was intentionally untracked; user chose to version-control the validated round-flow/layout/card-back fixes to avoid the prior lost-work failure mode. | Keep Rhum32 untracked (backups only); commit docs only. | If Rhum32 source is relocated or the repo's tracked-scope policy changes. `rhum32-server/` remains untracked for now. |
| 2026-05-18 | Rhum32 entry must funnel through the shared `runAd('pregame')`→`confirmEnter` flow (like SipSam's `enterTable`), not a bespoke `enterRhum32Table` direct-redirect. | The bespoke path skipped `/api/game/rhum32/enter`, so the bank was never drawn and exit credited nothing — a silent money bug. Shared flow keeps enter/exit symmetric across games. | Keep per-game custom entry functions. | If a game legitimately needs a different entry UX (then it must still call its `/enter` endpoint before redirect). |
| 2026-05-18 | Start tracking `rhum32-server/` in git too (supersedes the earlier "rhum32-server stays untracked"). | The IGM port put a `wallet-security-reviewer`-approved money-path change (`_onReplenishWallet`, token threading) in `rhum32-server/`; same lost-work rationale that led to tracking `rhum32-client`. `node_modules/` excluded via `.gitignore`. User approved. | Keep rhum32-server untracked (server money-path change unversioned). | If server source relocates or tracked-scope policy changes. |
| 2026-05-18 | Commit the Rhum32 money-path fix together with the 1-line Celestial display-mirror change already dirty in `vurglife-platform/client/public/index.html`. | Cannot stage a partial file non-interactively; the extra line is a cosmetic display-only mirror (server bills via `shared/sipsam-tables.js`), and splitting the money path across commits is worse than bundling one harmless cosmetic line. User approved. | Revert the Celestial line (discards prior in-flight work) or defer the dashboard fix (leaves money path half-committed). | If the Celestial mirror change turns out to be unwanted — revert it separately. |
